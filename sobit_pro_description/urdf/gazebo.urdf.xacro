<?xml version="1.0" encoding="UTF-8" ?>
<!-- SOBIT PRO (Gazebo) -->
<robot name="sobit_pro_gazebo" xmlns:xacro="http://ros.org/wiki/xacro">
  <!--
    guider: rendering sensors are always_on=0

    Every rendering sensor in this file is <always_on>0</always_on>, so gz
    only renders it while something is actually subscribed to its gz topic.
    That is what makes the Guider Robot Manager panel's per-sensor
    checkboxes (each starts/stops a ros_gz_bridge) a real throttle:
    unchecking one stops the GPU work, not just the ROS topic.

    With always_on=1 these rendered continuously for every spawned robot no
    matter what, so a robot the user had hidden (or "deleted", which parks
    the entity rather than destroying it) kept costing full render time for
    the rest of the session. That was a major reason real time factor
    decayed as robots were spawned and removed.

    Non rendering sensors (IMU, contact, force torque) are deliberately
    left alone: they cost almost nothing and switching them off would only
    risk starving something that depends on them.

    Applied by guider_setup_robots (guide_robot). Re running it is safe.
  -->
  <xacro:macro name="gazebo_sensors" params="robot_name enable_gz_head_cam_color:=True enable_gz_head_cam_depth:=True enable_gz_lidar:=True">

    <!-- Sensor - Head Camera (Color) -->
    <xacro:if value="${enable_gz_head_cam_color}">
      <gazebo reference="head_camera_link">
        <sensor name="head_camera_color" type="camera">
          <pose>0.0 0.0 ${0.0175/2} ${M_PI/2} -${M_PI/2} 0.0</pose>
          
          <gz_frame_id>${robot_name}/head_camera_link</gz_frame_id>
          <topic>${robot_name}/head_camera/color</topic>

          <always_on>0</always_on>
          <visualize>true</visualize>
          <update_rate>1</update_rate>

          <camera name="head_camera_color">
            <horizontal_fov>1.21126</horizontal_fov>
            <image>
              <width>640</width>
              <height>480</height>
              <format>RGB_INT8</format>
            </image>
            <clip>
              <near>0.1</near>
              <far>10</far>
            </clip>
          </camera>
        </sensor>
      </gazebo>
    </xacro:if>

    <!-- Sensor - Head Camera (Depth) -->
    <xacro:if value="${enable_gz_head_cam_depth}">
      <gazebo reference="head_camera_link">
        <sensor name="head_camera_depth" type="depth">
          <pose>0.0 0.0 ${0.0175/2} ${M_PI/2} -${M_PI/2} 0.0</pose>

          <gz_frame_id>${robot_name}/head_camera_link</gz_frame_id>
          <topic>${robot_name}/head_camera/depth</topic>

          <always_on>0</always_on>
          <visualize>true</visualize>
          <update_rate>1</update_rate>

          <camera name="head_camera_depth">
            <horizontal_fov>1.48702</horizontal_fov>
            <image>
              <width>640</width>
              <height>480</height>
            </image>
            <clip>
              <near>0.1</near>
              <far>10</far>
            </clip>
          </camera>
        </sensor>
      </gazebo>
    </xacro:if>

    <!-- Sensor - LiDAR -->
    <xacro:if value="${enable_gz_lidar}">
      <gazebo reference="lidar_laser">
        <sensor name="lidar" type="gpu_lidar">
          <pose>0 0 0 0 0 0</pose>

          <gz_frame_id>${robot_name}/lidar_laser</gz_frame_id>
          <!-- <qos>
            <topic name=${robot_name}/scan>
              <publisher>
                <reliability>best_effort</reliability>
                <durability>volatile</durability>
                <history>keep_last</history>
                <depth>5</depth>
              </publisher>
            </topic>
          </qos> -->
          <topic>${robot_name}/scan</topic>

          <always_on>0</always_on>
          <visualize>true</visualize>
          <update_rate>10</update_rate>

          <ray>
            <scan>
              <horizontal>
                <samples>640</samples>
                <resolution>1</resolution>
                <!-- <min_angle>-2.4870942</min_angle>
                <max_angle>2.4870942</max_angle> -->
                <min_angle>-1.5708</min_angle>
                <max_angle>1.5708</max_angle>
              </horizontal>
              <vertical>
                <samples>1</samples>
                <resolution>0.01</resolution>
                <min_angle>0</min_angle>
                <max_angle>0</max_angle>
              </vertical>
            </scan>
            <range>
              <min>0.10000000149011612</min>
              <max>10.0</max>
              <resolution>0.01</resolution>
            </range>
          </ray>
        </sensor>
      </gazebo>
    </xacro:if>

    <!-- Publish Joint States -->
    <gazebo>
      <plugin filename="gz-sim-joint-state-publisher-system" name="gz::sim::systems::JointStatePublisher">
        <topic>${robot_name}/joint_states</topic>
      </plugin>
    </gazebo>

  </xacro:macro>
</robot>
